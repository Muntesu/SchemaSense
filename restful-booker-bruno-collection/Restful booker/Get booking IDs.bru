meta {
  name: Get booking IDs
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/booking
  body: none
  auth: inherit
}

params:query {
  ~firstname: 
  ~lastname: 
  ~checkin: 
  ~checkout: 
}

tests {
  test("should return 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
