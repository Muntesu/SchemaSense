meta {
  name: Get booking
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/booking/
  body: none
  auth: inherit
}

tests {
  test("should return 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
